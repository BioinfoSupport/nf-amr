

manifest {
    name            = 'amr-genomics/nf-amr'
    author          = 'Julien Prados, Fedor Bezrukov'
    homePage        = 'https://gitlab.unige.ch/nf-amr'
    description     = 'Annotation of AMR bacterial genomes.'
    mainScript      = 'main.nf'
    nextflowVersion = '>=23.04.2'
    version         = 'v1.4.1'
}


params {
	run_prokka = true
	species_flags_xlsx = 'species_flags.xlsx'
	input = 'data/*.fasta'
}


includeConfig 'conf/orgs_flags.config'


profiles {
    standard {
        docker {
            enabled = true
            runOptions = "--user \$(id -u):\$(id -g) --group-add 100"
        }
    }

    // using singularity instead of docker
    singularity {
        singularity {
            enabled = true
            autoMounts = true
        }
    }
}

